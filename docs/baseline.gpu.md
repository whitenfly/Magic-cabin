# 性能基线（J0.6 / F0.6）

> **本文件由 `node tests/e2e/perf.mjs --update` 生成** —— 不要手改数值；
> 需要更新基线时重跑脚本，并**人工确认差异来源**后再提交。
>
> 采集时间：2026-09-13T14:14:16.329Z ｜ 环境：**本机 / 真实 GPU** ｜ 轮次：5（取中位数）

---

## 0. 一句话

**主判据是渲染统计（`calls` / `triangles` / `geometries` / `textures`），不是 FPS。**

它们只取决于**场景构成**，与 GPU 或软件渲染无关；而 FPS 由运行环境支配 ——
它只能说明"同一台机器上变快了还是变慢了"，不能说明"性能好不好"。

---

## 1. 结论摘要

| 指标 | 值（中位数） | 区间 | 可比性 |
|---|---|---|---|
| 首屏时间（**稳态 / 热**） | **504 ms** | 491–549 ms | 同环境前后对比 |
| 首屏时间（**冷启动 / 首访**） | 853 ms | 单次采样 | 同环境前后对比 |
| boot 逻辑耗时（热） | 468 ms | 447–513 ms | 同环境前后对比 |
| 稳态 FPS | 78.05 | 75.78–84.49 | ⚠️ **仅同环境** |
| 冷启动 FPS（参考） | 105.62 | 单次采样 | ⚠️ 仅同环境 |
| **draw calls** | **3138** | — | ✅ 跨设备可比 |
| **triangles** | **86158** | — | ✅ 跨设备可比 |
| geometries | 2843 | — | ✅ 跨设备可比 |
| textures | 38 | — | ✅ 跨设备可比 |
| 着色器程序数 | 15 | — | ✅ 跨设备可比 |
| 场景对象数 | 5365（Mesh 1812 / Line 1907 / Points 7） | — | ✅ 跨设备可比 |

### 1.5 为什么要把"冷启动"单独列出来

同一个浏览器进程里，**第一次加载**要额外付出：V8 编译与首次 JIT、GPU 上下文与 shader 初始化、
几何/纹理首次上传、资源首取。后续轮次共享这些缓存，所以又快又稳。

两者不是同一件事，混在一起统计会**两头失真**：

| | 代表什么 | 该怎么用 |
|---|---|---|
| **冷启动**（第 0 轮） | 用户**第一次打开**页面的真实体验 | 看首访体验；优化首屏时盯这个 |
| **稳态 / 热**（第 1…N 轮） | 页面**已经在跑**时的表现 | 看 FPS 与渲染开销；回归判定用这个 |

所以脚本会先单独跑一轮冷启动（`第 0 轮`），**不计入**稳态统计；
`calls` / `triangles` 与冷热无关（同一场景），两者必然相同。


---

## 2. 采集环境（**决定这些数字能不能横向比**）

| 项 | 值 |
|---|---|
| 采集 profile | `gpu`（本机 / 真实 GPU） |
| 渲染后端 | ANGLE (NVIDIA, NVIDIA GeForce RTX 5060 Laptop GPU (0x00002D59) Direct3D11 vs_5_0 ps_5_0, D3D11) |
| **后端判定** | **硬件加速** |
| 浏览器 | Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) HeadlessChrome/153.0.0.0 Safari/537.36 |
| 视口 / 像素比 | 1440×900 @ 1x |
| 场景种子 | 20260214 |
| 页面 URL | `http://127.0.0.1:5173/index.html?deterministic=1&stats=1` |

> ⚠️ FPS 与首屏时间**只在本机、本环境下有意义**：换机器、换后端后必须**重建基线**，不能拿旧数字做判定。
> 渲染统计（calls / triangles / geometries / textures）不受此影响。

---

## 2.5 采集质量

| 项 | 值 |
|---|---|
| FPS 轮间波动（极差/中位数） | 11% |
| 首屏时间轮间波动 | 12% |
| FPS 逐轮趋势（末轮 vs 首轮） | -10% |
| 判定 | ✅ 稳定 —— 轮间波动与趋势均在正常范围 |

> 采集期间若机器上还有别的程序在抢 GPU / CPU，FPS 与首屏时间会被拉低且轮间抖动；
> 持续满载也会让笔记本降频，表现为 FPS 逐轮下滑。
> **判据（渲染统计）不受影响** —— 它们是场景的确定性属性，与负载无关。

---

## 3. 怎么用（判定性能回归）

```bash
pnpm serve          # 前置
pnpm test:perf      # 与本 profile 的基线比对；任一指标超过阈值即非零退出
```

阈值（超过即报警）：

| 指标 | 阈值 | 方向 | 性质 |
|---|---|---|---|
| `calls` | 10% | 上升 | **判据** |
| `triangles` | 10% | 上升 | **判据** |
| `geometries` | 10% | 上升 | **判据** |
| `textures` | 10% | 上升 | **判据** |
| `sceneObjects` | 10% | 上升 | **判据** |
| `readyMs` | 30% | 上升 | ⚠️ 参考项（噪声大） |
| `fps` | 25% | 下降 | ⚠️ 参考项（噪声大） |

> **为什么分两档**：渲染统计只取决于场景构成 —— 同一份代码连测多轮**完全一致（+0.0%）**，
> 所以 10% 是宽松但有效的门；而首屏时间与 FPS 受运行环境支配，
> 同一份代码批次间波动实测可达 ±15%（首屏单轮甚至 693–1390 ms），阈值必须放宽，否则会天天误报。

含义：**搬迁式改动（`J2`–`J4`）不应该让 draw call 数或三角面数上升超过 10%**。
如果上升了，说明新架构引入了额外的渲染批次（例如材质/几何不再共享、光照槽位重复提交）——
这正是这份基线存在的理由。

---

## 4. 采集口径（复现时不要改这些）

| 项 | 值 | 为什么 |
|---|---|---|
| 预热 | 3 s | 让着色器编译、首批纹理生成、天气/光照过渡完成 |
| FPS 采样窗口 | 5 s | 太短会被单帧抖动支配 |
| 轮次 | **1 轮冷启动（不计入统计）+ 5 轮稳态**，稳态取**中位数** | 冷启动与稳态不是同一件事，混在一起两头失真（见 §1.5） |
| 轮间隔 | 1 s | 让上一轮的收尾（GC / 上传）落定 |
| 时钟模式 | **realtime**（`?deterministic=1`，不带 `frames`） | manual 模式不跑 rAF，量不到 FPS |
| 统计钩子 | `?stats=1` | `renderer.info` 在 IIFE 内，需要钩子才能读到 |
| 渲染后端 | 真实 GPU（不加任何 GPU 开关，让 Chrome 自选） | 决定 FPS / 首屏时间能否横向比 |

---

## 5. 原始数据

- 机器可读：[`../tests/e2e/baseline/perf.gpu.json`](../tests/e2e/baseline/perf.gpu.json)
- 冷启动（第 0 轮，单次）：首屏 853 ms · FPS 105.62 · calls 3138 · triangles 86158
- 稳态（第 1–5 轮，取中位数）：

- 第 1 轮：首屏 506 ms · FPS 84.49 · calls 3138 · triangles 86158
- 第 2 轮：首屏 491 ms · FPS 82.49 · calls 3138 · triangles 86158
- 第 3 轮：首屏 502 ms · FPS 78.05 · calls 3138 · triangles 86158
- 第 4 轮：首屏 549 ms · FPS 75.78 · calls 3138 · triangles 86158
- 第 5 轮：首屏 504 ms · FPS 76.4 · calls 3138 · triangles 86158

---

## 6. 它和另外两条安全网的关系

| 安全网 | 命令 | 回答的问题 |
|---|---|---|
| 截图回归（`J0.4`） | `pnpm test:visual` | 画面**有没有被改坏** |
| 交互冒烟（`J0.5`） | `pnpm test:smoke` | 还能**不能玩** |
| **性能基线（`J0.6`）** | `pnpm test:perf` | 有没有**悄悄变慢** |

详见 [`F0.4-实施结果.md`](./实施结果/F0.4-实施结果.md) / [`F0.5-实施结果.md`](./实施结果/F0.5-实施结果.md) / [`F0.6-实施结果.md`](./实施结果/F0.6-实施结果.md)。
