# J4.45 · `floor2/wand` 升格 `defineProp` —— **任务 D 收官** —— 实施结果

> 编号 **`J4.45`** ｜ 分支 `task/J4.45-floor2-wand-impl` ｜ `--base=0.4.0`
>
> 按 `J4.44` 施工图执行。18.9 整段 **610 行**（任务 D 最大的一件）一次搬完，
> **段导出清单的私有导出归零** ⇒ 任务 D 的 **15 组全部关闭**。

---

## 0. 一句话结论

魔法杖升格为 `world/floor2/wand.js`：**16 个子块逐字搬运**（材质 · 魔杖本体 · 六种元素 ·
5 个几何工具 · 魔法阵 · 实体火焰 · 六种造物 · 冰棱 · 岩石 · 闪电 · 五段状态机）+ 1 条交互。

> ★ **610 行的搬运，画面 3/3 机位 sha256 逐字节相同**，且 `verify-migration.mjs` 的
> **八项计数全部零偏移**。
> ★ **段导出清单里属于已搬物件的导出全部清零**（本件删最后 11 项）。
> ★ `install.js` 由 **1057 → 465 行**（净 −592）。

---

## 1. 16 个子块（按 `J4.44` §1 的顺序）

| # | 子块 | 备注 |
|---|---|---|
| 1 | 材质 `wandWoodMat` / `wandDarkMat` / `wandEdgeMat` | |
| 2 | `woodPart(g, mat)` | 木纹部件工厂 |
| 3 | `hookArcPts` + 两个挂钩 | ★ **build 期 3 处 `scene.add`**（`hook` / `outline`）|
| 4 | 魔杖本体 `wandG` | ★ 第 4 处 build 期 `scene.add` |
| 5 | `wandGlowSphere` + `ELEMENTS`（六种元素） | |
| 6 | 5 个几何工具 | `loopLine` / `openLine` / `ringPts2` / `polyPts2` / `starPts2` / `spiralPts2` |
| 7 | `buildMagicCircle(el, idx)` | 魔法阵（六套纹样，**运行期动态建 + 动态移除**）|
| 8 | `makeSolidFlame(h, w, color, phase, speed)` | 实体火焰 |
| 9 | `buildCreation(idx, el)` | **六种造物**（火/水/冰/土/雷/风）|
| 10 | `iceShard(r, h, tilt)` | 冰棱（嵌套在 `idx===2` 分支内）|
| 11 | `makeRock(r, amp)` | 岩石（用共享工具 `jitterGeo`）|
| 12 | `genBolt(pair)` | 闪电（**6 处 `runtimeRng`**）|
| 13 | `crystalColor` / `crystalTarget` / `aimQ` / `IDENTITY_Q` | |
| 14 | `wandState` + `regMagic` | → `state` + `parts.tryCast` |
| 15 | `clearCast()` | 2 处 `scene.remove` |
| 16 | `updateWand2(time)` | ~80 行五段状态机 |

---

## 2. `J4.44` 施工图列的"三件事"，逐条兑现

| 件 | 施工图要求 | 本件做法 |
|---|---|---|
| `clock.now` | 仅一处（`wandState.t0 = clock.now`）| → `s.t0 = s.now`；`update` 第一行写 `s.now = time` ✅ **全件只有这一处** |
| `runtimeRng` 的**次数与顺序** | 9 处全在运行期；点击后的调用顺序逐字保持 | ✅ 未改一处：`buildCreation` 火星 9×1 · `genBolt` 3+3×5 · `tryCast` 1；**调用顺序与原来完全相同** |
| `scene.add` / `scene.remove` | build 期 3 处留原位；运行期 4 处逐字 | ✅ build 期 `hook`/`outline`/`wandG` 在原位；运行期 `circleHolder`/`crea` 的加入与移除逐字 |

> ★ 施工图 §0 "不可分段搬迁"的判断也被验证：整段一次搬完，`tsc` 一次通过。

---

## 3. ★ `ctx` 导出的清理（施工图 §3 的核对结果落地）

18.9 段原先在 `ctx` 上挂了 **25 个键**；按施工图的核对结果，本件搬走后**全部消失**
（唯一的外部读者 `ctx.updateWand2` 由 `FrameBody` 改走 `prop/wand`）。

**段导出清单再删 11 项**：

| 原行 | 删除的项 |
|---|---|
| L69 | `woodPart` · `wandGlowSphere` · `ringPts2` · `polyPts2` · `starPts2`（**整行 5 项**）|
| L76 | `spiralPts2` · `buildMagicCircle` · `makeSolidFlame` · `buildCreation`（**整行 4 项**）|
| L77 | `clearCast` · `updateWand2`（2 项，**保留**同行的 `cbox` / `crboxCol` / `crumpleBall` / `arcPos`）|

⇒ ★ **段导出清单现在只剩 4 项共享工具** —— 这一类"悬空导出"的债
（本项目因它出错过 **8 次**）**全部清完**：`J4.36` 起从 6 项一路降到 **0 项私有导出**。

---

## 4. 门禁（七项全绿 · 实测）

| # | 门禁 | 实测结果 |
|---|---|---|
| 1 | `tsc --noEmit` | ✅ **exit 0** |
| 2 | `node scripts/_verify.mjs` | ✅ **58 项通过 / 0 项失败** |
| 3 | `astro build` | ✅ exit 0，8 page(s) built |
| 4 | `tests/visual/compare.mjs` | ✅ ★ **3/3 机位 sha256 逐字节相同** |
| 5 | `tests/e2e/smoke.mjs` | ✅ **32 通过 / 0 失败**（215.4s）|
| 6 | `tests/e2e/perf.mjs --compare` | ✅ **全部指标在阈值内** |
| 7 | `tests/e2e/j3-probe.mjs` | ✅ **7 通过 / 0 失败** |

### 4.1 ★ `verify-migration.mjs` 的八项计数**全部零偏移**

| 计数 | 实测 | 说明 |
|---|---|---|
| 裸 `Math.random()` | **0 处** | ✅ |
| `Math.random()` | **286 → 286** | ★ 行注释里**没有**写带括号的 `runtimeRng`（施工图 §7 的警告生效）|
| `new THREE.Mesh(` | **184 → 184** | ✅ 11 个子块整体搬家，条数守恒 |
| `new THREE.Group(` | **198 → 198** | ✅ |
| `regMagic(` | 64 → 29（**−35**）| 与 `J4.43` **完全一致** —— 见下 |
| `addEventListener(` | 51 → 49（**−2**）| 与 `J4.43` 携带 `NoteEditor` 的 2 个监听时一致 ⇒ 本件**未动** |
| `getElementById(` | 49 → 41（**−8**）| 同上 |
| `SND.play(` | 22 → 19（**−3**）| 同上 |

> ★ **`regMagic(` 仍是 −35，这是预期内的**：本件**真实删掉 1 处**调用
> （`ctx.regMagic(wandG, …)`），但 `wand.js` 的**行注释**里写了同名文字量（解释搬运对照）
> ⇒ **恰好抵平**。这与施工图 §7 的预测（"若无注释字面量则 −36"）**方向一致**，
> 且与 `J4.43` 的白名单值相同 ⇒ **无需改动白名单**。
>
> ⚠️ 这条老坑（`stripComments` 只剥块注释、不剥行注释）在本项目已出现 **9 次** ——
> 但**这一次它是"事先算准"的**，不是事后排查。

### 4.2 `j3-probe` 关键数字

| 指标 | `J4.43` | 本件 | 变化 |
|---|---|---|---|
| `props` | 52 | **53** | **+1**（魔法杖）|
| `interactables` | 108 | **109** | **+1**（`wand/cast`）|
| `magicMeshes` | 744 | 744 | **0** |

`_j3-diag`：**捕获到的错误（0）**，`ready` / `state: running`。

### 4.3 性能明细

五项场景统计**全部 +0.0%**；`readyMs` 858 → 832（**−3.0%**，参考项）；
`fps` 4.15 → 3.93（−5.3%，参考项）。

⇒ 说明魔杖、两个挂钩、以及六种造物/魔法阵的 `scene.add` 数量与顺序都未变 ✓

---

## 5. 逐字搬运说明（关键数值）

状态机五段时长 `FLY 0.8 / GROW 0.5 / HOLD 2.3 / FADE 0.6 / RET 0.8`；
六种元素色值（`fire 0xff5a2a` · `water 0x3c8aff` · `ice 0xaef0ff` · `earth 0xc08a4a` ·
`bolt 0xffe94a` · `wind 0x7dffb8`）；
`genBolt` 的 `nSeg 5` / `jitter 0.05·(1−t·0.5)` / `len 0.20 + rng×0.12` / 重生成间隔 `0.15`；
三处插值系数 `lerp 0.08` / `slerp 0.06` / `slerp 0.07`；
魔杖几何（`Cylinder 0.014/0.022/0.72` · `0.008/0.014/0.30` · 球 `0.034` · 环 `0.030/0.008`）；
水晶 `Octahedron 0.055` + `scale(0.8,0.8,1.9)` + 内核 `Sphere 0.018`；
两根挂钩 `Torus 0.058/0.011` + 22 点弧线；`WAND_REST / WAND_HOVER / CAST_POS` 三个坐标。

**`rng`（`N8`）**：9 处 `runtimeRng()` 全在运行期，`build` 期**一次都不消耗**
⇒ 装配位置对随机数序列无约束（与 `board` 的 1020 次 `textureRng` 相反）。

---

## 6. 落点

| 文件 | 动作 |
|---|---|
| `src/cabin/world/floor2/wand.js` | **新增**：魔法杖物件模块（16 子块 + 五段状态机）|
| `src/cabin/world/floor2/install.js` | 18.9 整段（**611 行**）→ 一行装配 ⇒ **1057 → 465 行**；段导出清单删最后 11 项 |
| `src/cabin/app/scene/FrameBody.js` | `frame/58` → `prop/wand`（唯一一条帧任务，无合并）|
| `tests/baseline/cabin-digest.json` | `verify-j15 --record`（J4 合法改动）|
| `docs/实施结果/J4.45-实施结果.md` | **新增**：本文 |
| `docs/实施结果/J4.32-实施结果.md` | 台账同步（**组 15 → ✅ 关闭**；§2 任务表）|
| `package.json` | `version` → `0.4.0-dev.44`（R9）|
