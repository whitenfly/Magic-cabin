# J3.3 实施结果 · 规范修订 SPEC-1.0.0 → SPEC-1.0.1

> 任务：`J3.3`｜分支：`task/J3.3-spec-pnpm-env`｜开发版 tag：`v0.3.0-dev.4`
> 依据：[`../VERSIONING.md`](../VERSIONING.md) **§12.2 修订 SOP**（本次是它的首次完整执行）
> 性质：**规范修订任务**（提交前缀 `spec:`），与任何功能改动**不混在同一分支/提交**（R14）

---

## 0 · 立项信息（§12.2 步 1）

| 项 | 内容 |
|---|---|
| 编号 | `J3.3` —— §2.3 四条查重命令**全部为空**（tag / 提交 / 文档 / 分支） |
| 递增类型 | **补丁位**（§12.3「措辞澄清、补漏、修错别字，**不改变任何规则含义**」） |
| 触发来源 | 用户在普通终端提问「`pnpm.cmd git:status` 还能用吗」，实测暴露了一个**未被规范记录**的环境根因 |
| 为什么不能顺手改 | R14 + §12.5 第 4 条：`J3.2` 定稿后规范进入冻结期，缺陷必须**另开 `spec:` 任务**修 |

---

## 1 · 影响面清单（§12.2 步 2 ★ 必填）

### ① 受影响的章节

| 位置 | 改动 |
|---|---|
| 文件标题 + 元信息表 | `SPEC-1.0.0` → `SPEC-1.0.1`（标题行、`规范版本` 行） |
| **§11.1** | 表格首行（`pnpm <别名>` 启动失败）改为指向新的 §11.1.1 |
| **§11.1.1**（新增） | 根因三条实测事实 + 开工前判别命令（两条）+ 降级结论 + **「禁止用 `CI=true` 绕过 TTY 中止」操作警告** |
| §12.1 | 冻结声明里的当前版本号同步为 `1.0.1` |
| §12.4 | 追加 `1.0.1` 行（**只增不改**） |

### ② 会被重新解读的既有 tag / 分支 / 文档

| 对象 | 结论 |
|---|---|
| 既有 tag（`v0.1.5` … `v0.3.0-dev.3`） | **无影响**——本次不触碰任何编号、序列、命名或分支规则 |
| 既有任务（`J2.x` / `J3.1` / `J3.2`） | **无影响**——它们的合规性判据完全未变 |
| `scripts/**` | **无影响**——本次脚本零改动 |
| `docs/实施结果/` 既有文档 | **无影响** |
| 历史判例 P1–P6 | **无影响**——未删改任何判例（§12.5 第 3 条禁止） |

### ③ 与 `scripts/release.mjs` 的一致性影响（§12.2 步 3）

逐条核对 §11 命令表与脚本实际行为：

| 核对项 | 结果 |
|---|---|
| 子命令集合 | ✅ **零新增、零修改**（本次是纯文档修订） |
| §11 表格描述 | ✅ 与实际行为一致，无需改动 |
| §11.1.1 的适用对象 | ✅ 描述的是**环境**行为（PATH 上的 pnpm + `node_modules` 状态文件），**不是脚本行为** ⇒ 不存在"两边都要改"的同步问题 |

> 结论：**不触发** §12.2 步 3 的"先修脚本或先修文档"分歧。

### ④ 与 `VERSIONING-QA.md` 的同步点

| 动作 | 说明 |
|---|---|
| **新增 Q18** | 「为什么沙箱里 `pnpm` 会失败？会毁掉 `node_modules` 吗？」——读者向的根因解释 + 实测结论 |
| Q17 追加「首个执行先例」 | 记录 `1.0.1` 是 §12.2 流程的首次完整执行，并实证"先按现行规范做完、再另开任务修" |
| 顶部定位**去掉硬编码版本号** | 改为「当前版本与修订历史见其 §12.4」——**主动消除未来同步点**（§12.5 单一事实来源） |

---

## 2 · 人类确认（§12.2 步 4 ★ 硬门槛）

| 项 | 内容 |
|---|---|
| 确认方式 | 用户在会话中明确指示：**「我已完成推送，你可以把这个任务补进去了」** |
| 确认时点 | 2026-09-16（在用户完成推送、经 `status` 复查确认 `dev`/`main` 均 0 ahead 之后） |
| 授权范围 | 批准 `SPEC-1.0.0 → 1.0.1` 的**补丁位**修订（即：允许本次按 §12.5 的禁令边界内补漏） |
| 模型自行扩大范围？ | ❌ 无。未越界到次版本位（无新增规则）、未删改判例、未动 §1 红线 |

> ⚠️ 本条是**文本留痕**而非签名。按 §12.2，人类确认是合并的**前置条件**，此处已满足。

---

## 3 · DoD 核对

| # | 要求 | 落点 | 状态 |
|---|---|---|---|
| 1 | 把 pnpm 失败的**根因而非现象**写进规范 | §11.1.1（三条实测事实） | ✅ |
| 2 | 给出**可执行的判别手段**（模型开工前能自查） | §11.1.1 的两条判别命令 | ✅ |
| 3 | 明确"降级路径是默认姿势"而非备选 | §11.1.1 结论段 | ✅ |
| 4 | 补上**防误操作**的警告（pnpm 自己给的建议是陷阱） | §11.1.1 的操作警告块 | ✅ |
| 5 | 走完 §12.2 全流程（立项 / 影响面 / 一致性 / 人类确认 / 递增 / 记录 / 独立提交） | 本文 + §12.4 | ✅ |
| 6 | 版本递增正确（补丁位） | `SPEC-1.0.1` | ✅ |
| 7 | Q&A 同步且**减少**未来同步点 | 新增 Q18；顶部去硬编码 | ✅ |

---

## 4 · 落点（文件清单）

| 文件 | 动作 |
|---|---|
| `docs/VERSIONING.md` | 修订：标题/元信息版本号、§11.1 表格行、**新增 §11.1.1**、§12.1 版本号、§12.4 追加 `1.0.1` |
| `docs/VERSIONING-QA.md` | 修订：新增 Q18、Q17 追加先例、顶部去硬编码版本号 |
| `docs/实施结果/J3.3-实施结果.md` | 新增（本文） |
| `package.json` | 修订：`version` → `0.3.0-dev.4` |

---

## 5 · 门禁项数（§6）

| # | 项 | 命令 | 结果 |
|---|---|---|---|
| 1 | 类型检查 | `node_modules/.bin/tsc --noEmit` | ✅ 通过 |
| 2 | 静态校验 | `node scripts/_verify.mjs` | ✅ 通过（7 脚本全过 / 0 跳过，含 `src/cabin/**` 逐字节摘要） |
| 3 | 构建 | `node_modules/.bin/astro build` | ✅ 通过（8 页） |
| 4–6 | visual / smoke / perf | —— | ➖ **不适用**：改动**不含 `src/**` 任何字节**，画面与运行时零差异由第 2 项的摘要件直接证明 |

> 门禁路径：`pnpm` 在本环境不可用（本次修订的主角），故按 §11.1 用**等价直调**。

---

## 6 · 遗留与交接

| # | 项 | 现状 |
|---|---|---|
| **L4** ★ | **`J3` 阶段仍未发布 `v0.3.0`** | `main` 停在 `v0.2.5`；`dev` 已累积 `J3`+`J3.1`+`J3.2`+`J3.3`。**下一步应做 §5.2 阶段发布**，并跑 §5.3 的四处状态副本清单 |
| L1 | `dev.N` 序号算法偏差（数量+1 vs 最大+1） | 规范由 R8 兜住；工具侧待修（属 `spec:` 类） |
| L2 | `package.json` 版本仍靠手工同步 | 待工具侧改进 |
| L3 | `pre-commit` 在受限沙箱崩溃并阻塞提交 | 已记入 §11.1；普通终端正常，属环境边界 |
| **L7**（新） | 本环境 `.modules.yaml` 仍缺失 | 建议在**普通终端**跑一次 `pnpm install` 补写；不补则每次 pnpm 都重复触发 install（不影响功能） |
| L5 | 仓库根 4 个 `_tmp-j3f-*.mjs` | 待清理，需单开编号 |

---

## 7 · 流程自证

| 步 | 实际执行 | 结果 |
|---|---|---|
| §12.2 ① 立项 | §2.3 四条查重 → `start J3.3 spec-pnpm-env` | ✅ |
| §12.2 ② 影响面清单 | 本文 §1（四项齐全） | ✅ |
| §12.2 ③ 一致性核对 | 脚本零改动，§11 表无需同步 | ✅ |
| §12.2 ④ 人类确认 | 本文 §2 | ✅ |
| §12.2 ⑤ 版本递增 | `SPEC-1.0.1`（补丁位） | ✅ |
| §12.2 ⑥ 记录 | `§12.4` 追加一行（只增不改） | ✅ |
| §12.2 ⑦ 独立提交 | 独立分支 + `--no-ff`，前缀 `spec:` | ✅ |
