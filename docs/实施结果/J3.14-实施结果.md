# J3.14 实施结果 · 规范修订 SPEC-1.2.0 → SPEC-1.3.0（PowerShell BOM 坑）

> 任务：`J3.14`｜分支：`task/J3.14-spec-bom-guard`｜开发版 tag：`v0.3.0-dev.14`
> 依据：[`../VERSIONING.md`](../VERSIONING.md) **§12.2 修订 SOP**（第六次完整执行）
> 主题：**L21** —— 把「PowerShell 改文件会加 BOM」写进规范
> 性质：**规范修订**任务（提交前缀 `spec:`），与任何功能改动不混在同一分支/提交（R14）

---

## 0 · 立项信息（§12.2 步 1）

| 项 | 内容 |
|---|---|
| 编号 | **`J3.14`** —— §2.3 查重四条全空 |
| **递增类型** | ★ **次版本位** `1.2.0 → 1.3.0` —— 新增一条**禁忌**（与 L12/§8.5 同类），不改变任何红线 |
| 窗口 | `J3` 已发布、`J4` 未开工 ⇒ 仍是 §12.5 第 4 条允许改规范的**唯一窗口** |
| 触发 | `J3.12` 清理 `GitPushHistory.md` 时实测踩中（前 3 字节 `35,32,71` → `239,187,191`），已用 node 重写剥离 |

---

## 1 · 影响面清单（§12.2 步 2 ★ 必填）

### ① 受影响的章节

| 位置 | 改动 |
|---|---|
| 文件标题 + 元信息表 + §12.1 | `SPEC-1.2.0` → `SPEC-1.3.0`（三处版本号同步） |
| **§8.6（新增）** | 「用 PowerShell 改文件会**加 BOM**」——现象 / 为什么对本仓库致命（逐字节判据）/ 正误写法对照（node 或 `-Encoding utf8NoBOM`）/ **三字节复核判据** / 判例 P8 |
| 附录 B | 补入判例 **P8**（仅追加，未删改既有判例） |
| §12.4 | 追加 `1.3.0` 行（**只增不改**） |

### ② 会被重新解读的既有 tag / 分支 / 文档

| 对象 | 影响 | 结论 |
|---|---|---|
| 既有 `v*` tag（29 个） | **无** | 编号/序列/命名规则未变 |
| `GitPushHistory.md` | **无**（BOM 已在 `J3.12` 剥离，现为无 BOM） | ✅ 该文件在 `.gitignore` 内、不受判据保护，本修订只防未来 |
| `tests/baseline/*.json` / `*.png` 等判据文件 | **无** | 本次未触碰；本修订的意义正是**保护它们**免受未来误操作 |
| 判例 P1–P7 | **无删改** | 仅**追加** P8 |
| `scripts/**` | **无** | 零改动（纯文档） |

### ③ 与 `scripts/release.mjs` 的一致性影响（§12.2 步 3）

| 核对项 | 结果 |
|---|---|
| 子命令集合 | ✅ **零改动** |
| §8.6 的适用对象 | ✅ 描述的是 **PowerShell 的文件写入行为**（与工具无关）⇒ 无同步问题 |
| §8.5 / §8.6 的关系 | ✅ 同属"PowerShell 操作陷阱"，并列两节，**互不重复**（§12.5 单一事实来源） |
| §11 / §11.1 | ✅ 无需改动 |

### ④ 与 `VERSIONING-QA.md` 的同步点

| 动作 | 说明 |
|---|---|
| **新增 Q21** | 「用 PowerShell 改文件，为什么会让门禁'报错却查不出原因'？」——含现象、为什么致命、两条正确写法、复核判据、判例 P8 |
| 顶部定位 | 已改为「版本与修订历史见 §12.4」 ⇒ **本次无需改动** |

---

## 2 · 人类确认（§12.2 步 4 ★ 硬门槛）

| 项 | 内容 |
|---|---|
| 确认方式 | 用户在会话中明确指示：**「修 L20、L21」** |
| 前提 | 模型已在上一轮说明：L20 属工具（`J3.13`）、**L21 属规范修订**（需独立列 `spec:` 任务，R14），以及两者都要赶在 `J4` 开工前 |
| 授权范围 | 批准 L21 的**次版本位**修订（`1.2.0 → 1.3.0`） |
| 模型自行扩大范围？ | ❌ 无。未删改判例（P8 是追加）、未动 §1 红线、版本位从严（按"新增禁忌"升次版本位） |

---

## 3 · DoD 核对

| # | 要求 | 落点 | 状态 |
|---|---|---|---|
| 1 | BOM 坑写进规范 | §8.6（含正误对照 + 三字节复核判据） | ✅ |
| 2 | 说明**为什么对本仓库致命** | §8.6 点明它与 §8 逐字节判据的关系（"报警却查不出原因"） | ✅ |
| 3 | 给出可直接复制的正确写法 | node 一行式 + `-Encoding utf8NoBOM` | ✅ |
| 4 | 判例留痕 | 附录 B 追加 P8 | ✅ |
| 5 | 版本位正确（新增禁忌 ⇒ 次版本位） | `SPEC-1.3.0`，三处版本号同步 | ✅ |
| 6 | Q&A 同步 | 新增 Q21 | ✅ |
| 7 | 走完 §12.2 七步 | 本文 + §12.4 | ✅ |

---

## 4 · 落点（文件清单）

| 文件 | 动作 |
|---|---|
| `docs/VERSIONING.md` | 修订：**新增 §8.6**、附录 B 补 P8、§12.4 追加；标题/元信息表/§12.1 → `1.3.0` |
| `docs/VERSIONING-QA.md` | 修订：新增 Q21 |
| `docs/实施结果/J3.14-实施结果.md` | 新增（本文） |
| `package.json` | 修订：`version` → `0.3.0-dev.14` |

---

## 5 · 门禁项数（§6）

| # | 项 | 结果 |
|---|---|---|
| 1 | typecheck | ⬜ 见下 |
| 2 | verify | ⬜ 见下 |
| 3 | build | ⬜ 见下（**现已走直调 + 预清理**，不再 EPERM —— `J3.13` 的成果） |
| 4–6 | visual / smoke / perf | ➖ **不适用**：未触碰 `src/**`，由第 2 项的 `src/cabin/**` 逐字节摘要直接证明 |

---

## 6 · 遗留与交接

| # | 项 | 现状 |
|---|---|---|
| **L21** | PowerShell BOM 坑 | ✅ **本次清掉** |
| **L20 / L19** | 门禁 build 的 EPERM | ✅ 已由 `J3.13` 清掉 |
| L17 | `ship main` 不能代劳另起补丁版序列 | 建议不做（复杂度与收益不成比例） |
| L3 | `pre-commit` 受限沙箱崩溃 | 环境边界，无需执行 |
| **L8** | 待推送：`dev` 领先 6 + 新 tag | 由 `ship` 生成、人工推送 |

> **遗留清单再次清零**（仅剩"建议不做"与"环境边界"两类，以及待推送）。

---

## 7 · 流程自证（§12.2 七步）

| 步 | 实际执行 | 结果 |
|---|---|---|
| ① 立项 | §2.3 查重 → `start J3.14 spec-bom-guard` | ✅ |
| ② 影响面清单 | 本文 §1（四项齐全） | ✅ |
| ③ 一致性核对 | 脚本零改动；§8.5/§8.6 并列且互不重复 | ✅ |
| ④ 人类确认 | 本文 §2 | ✅ |
| ⑤ 版本递增 | `SPEC-1.3.0`（次版本位） | ✅ |
| ⑥ 记录 | §12.4 追加一行（只增不改） | ✅ |
| ⑦ 独立提交 | 独立分支 + `--no-ff`，前缀 `spec:` | ✅ |
