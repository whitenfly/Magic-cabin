# J4.24 · `floor1/magic-circle` 升格 `defineProp` —— 实施结果

> 编号 **`J4.24`** ｜ 分支 `task/J4.24-magic-circle-prop` ｜ `--base=0.4.0`
>
> **`J4.18` 契约的第三个真实用户**（光源**槽位 2**）。结构与 `J4.22`（槽 0）/ `J4.23`（槽 7）同款。

---

## 0. 一句话结论

紫色魔法阵升格为 `world/floor1/magicCircle.js`：几何 + 1 条交互 + 两段合并的 `update`
+ **`lights()` 声明的槽位 2**。

`J3` 判定"两条路都破判据"（搬走 ⇒ `mcRun` 消失 ⇒ `ReferenceError`；重注册 ⇒ 槽位从 2 变 8）
—— `slot: 2` 显式声明之后**两条路都通了**。画面 **3/3 机位逐字节相同**。

---

## 1. 病根

```js
ptMc += ((mcRun > 0 ? 1 : 0) - ptMc) * 0.055;   // ← 住在替换区间之外（「室内点光源」5 行之一）
```

---

## 2. 做了什么（1 个新文件 + 4 处改动）

| 文件 | 动作 |
|---|---|
| `world/floor1/magicCircle.js` | **新增**：几何（含 96 次 rng）+ 1 条 `interactables` + 两段合并的 `update` + `lights()`（`slot: 2`） |
| `world/floor1/install.js` | 删原 118 行几何段 → `installProp(magicCircle)`（原位置）；新增 import |
| `app/scene/FrameBody.js` | `frame/38` 改成调 `magicCircleApi.tick`；**删除** `frame/94`（强度平滑） |
| `world/lights.js` | **删除** `floor1/magic-circle` 注册行（现在只剩槽 1 / 3 / 4 / 5 / 6） |
| `systems/weather/WeatherSystem.js` | 删除 `ctx.ptMc = 0` |

**局部改名**：`ctx.mcRun` → `s.run`、`ctx.ptMc` → `s.pt`、`ctx.mcXxx` → `parts.mcXxx`、
`ctx.MC_X/MC_Z` → `L.MC_X/L.MC_Z`；`update` 里那个粒子缩放的局部量 `sc` 改名 `scq`
（避开几何段里同名的 `sc` 工具函数语义、并保持可读）。
其余数值**一个没改**（五道圆环与段数、六芒/三芒、12 根内刻度、24 组外刻度、6 处小圆、
六层悬浮阵的 `ty/spd/ph`、24 个八面体与配色表、命中面 `0.75/28`、
强度曲线 `0.12/0.82/0.18`、旋转 `0.15+2.8×inten`、粒子上升 `2.5`、平滑 `0.055`、时长 `8.0`）。

### 2.1 ⚠️ `rng` 顺序（本件最需要盯的一处）

`mcParts` 那 **24** 个八面体每个消耗 **4** 次 `floor1Rng()`（`a` / `r` / `ph` / `spd`）
⇒ 共 **96 次**。它们仍在 `build` 内、位置即原区间 ⇒ **次数与顺序都没变**
（不变量 `N8`）。画面 3/3 逐字节相同是这一条的直接证据。

### 2.2 ★ `userData.sfx` 留在 `build` 里

原 `mcG.userData.sfx = 'magic'` 必须仍在 `build` 中执行：`installProp` 的 aim 桥
**只在 `ud.sfx` 还不是字符串时**才补 `'toggle'` —— 写晚了音效就变
（与 `floor1/orrery` 同一处置，该件文件头也记了同一条）。

### 2.3 帧顺序：`ptMc` 的平滑提前，为什么等价

一件物件只有一个 `update`：几何分支在 `frame/38`、强度平滑在 `frame/94`
（`lightField.update` 是 `frame/97`）⇒ 本件选**几何分支的位置**（早）。论证同 `hangingLantern.js`：

1. **数值序列完全相同**：一阶低通，同帧同 `dt` 同输入 ⇒ 逐位相同；`grep mcRun` 的全部命中
   只有本物件（外加 `frame/97` 才被调用的 `lights()` 闭包）⇒ **两位置之间无读者/写者**；
2. **仍然早于读者**：本 `update` 在 `frame/38` 位置，远早于 `frame/97`。

---

## 3. 判据与证据

| 证据 | 结果 |
|---|---|
| **画面** | ✅ 3/3 机位 sha256 逐字节相同 ★ 核心判据（同时是 rng 顺序未变的证据） |
| **页面诊断** | ✅ `ready` / **0 个错误**（含**无** `LightField` 槽位警告） |
| **探针** | ✅ 7/7：`props=41`、`floor1/magic-circle` 在准星入口清单里、79 条交互各有 aim 入口 |
| **`magicMeshes=744`** | ✅ 与搬前一致 |

> 契约本身的有效性已由 `J4.22` §3.3 的**证伪实验**证明（改 `slot` ⇒ 画面红 1.8247%），
> 本件沿用同一契约、不重复破坏实验。

---

## 4. 门禁（六项 + 两条补充）

| # | 命令 | 结果 |
|---|---|---|
| 1 | `tsc --noEmit` | ✅ 0 错误 |
| 2 | `node scripts/_verify.mjs` | ✅ **58 项通过 / 0 失败**（含 §4.2） |
| 3 | `astro build` | ✅ 8 page(s) built |
| 4 | **画面回归** | ✅ **3/3 机位 sha256 逐字节相同** |
| 5 | 冒烟 | ✅ 见 §4.3 |
| 6 | 性能 | ✅ 见 §4.3 |
| ＋ | 页面诊断 | ✅ 0 个错误 |
| ＋ | 探针 `j3-probe` | ✅ 7 通过 / 0 失败 |

### 4.1 ⚠️ 预期红：`cabin-digest`

合法修改 4 个源文件 + 新增 1 个 ⇒ 随后重跑 `--record`（56 项通过）。
**本节即基线 `note` 要求的那"一笔原因"。**

### 4.2 `regMagic(` 计数白名单 `-24` → `-25`

删掉 1 处调用 ⇒ −1（实测 `64 → 39`）。起草时**一次到位**（措辞已固化成惯例）。

### 4.3 冒烟 / 性能

| 项 | 结果 |
|---|---|
| 冒烟 | ✅ **32 通过 / 0 失败**（214.8s，无未捕获页面异常） |
| 性能 | ✅ 全部指标在阈值内；`readyMs` −1.4%、`fps` −4.8%（均为参考项），其余**全部 +0.0%** |

---

## 5. DoD 核对

| 本任务自定 DoD | 结果 |
|---|---|
| 魔法阵整体升格（几何 + 交互 + 两段每帧） | ✅ `world/floor1/magicCircle.js` |
| ★ **光源随物件搬走、槽序不变** | ✅ `lights()` + `slot: 2`；画面 3/3 逐字节相同 |
| ★ `ptMc` 中间量消失 | ✅ 改为 `state.pt`；`WeatherSystem` 的初值同步删除 |
| ★ **96 次 `rng` 的顺序不变** | ✅ 画面逐字节相同（本件最强的间接证据） |
| 帧顺序约束被论证 | ✅ §2.3 两条（可 grep 复核） |
| 完整门禁六项全绿 | ✅（两处已知红按规范处置） |
| 实施结果文档含四节 | ✅ 本文 |

---

## 6. 遗留与交接

1. **`world/lights.js` 现在只剩 5 行注册**（槽 1 / 3 / 4 / 5 / 6）。
   已搬走：槽 0（`J4.22`）、槽 2（本任务）、槽 7（`J4.23`）。
2. **剩余 3 件一楼光源件**：`kotatsu`(槽 3) / `crystal-ball`(槽 4) —— 照本件照抄即可。
   `kotatsu` 另有跨件耦合（读 `cups` / `makeCup`，且 `_tv` 与提梁茶壶共用），详见 `J4.21` §6.1。
3. **`candle`(槽 5) / `magic-veil`(槽 6)** 在 `floor2`，其强度量 `candleP` / `magicP`
   住在 `floor2/install.js` 与 `FrameBody.js` —— 搬法同款，但**不在** `J4.15` §1.2 的清单里。
4. **`verify-migration.mjs` 的 `stripComments` 缺陷** —— 4 次实证，建议优先根治。

---

## 7. 落点

| 文件 | 动作 |
|---|---|
| `src/cabin/world/floor1/magicCircle.js` | **新增**：几何 + 1 条交互 + 两段合并的 `update` + `lights()`（`slot: 2`） |
| `src/cabin/world/floor1/install.js` | 删原 118 行几何段 → `installProp(magicCircle)`；新增 import |
| `src/cabin/app/scene/FrameBody.js` | `frame/38` → `magicCircleApi.tick`；**删** `frame/94` |
| `src/cabin/world/lights.js` | **删** `floor1/magic-circle` 注册行 |
| `src/cabin/systems/weather/WeatherSystem.js` | **删** `ctx.ptMc = 0` |
| `scripts/verify-migration.mjs` | ④ 的 `regMagic(` 白名单 `-24` → `-25` |
| `tests/baseline/cabin-digest.json` | **重跑 `--record`** |
| `docs/实施结果/J4.24-实施结果.md` | **新增**：本文 |
| `package.json` | `version` → `0.4.0-dev.23`（R9） |
