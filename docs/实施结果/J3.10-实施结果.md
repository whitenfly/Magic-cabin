# J3.10 实施结果 · 规范修订 SPEC-1.1.0 → SPEC-1.2.0

> 任务：`J3.10`｜分支：`task/J3.10-spec-powershell-qa`｜开发版 tag：`v0.3.0-dev.10`
> 依据：[`../VERSIONING.md`](../VERSIONING.md) **§12.2 修订 SOP**（第五次完整执行）
> 主题：**L12**（管道截断坑）+ **L13**（查重 ② 误报）+ **L14**（收尾结果回填路径）
> 性质：**规范修订任务**（提交前缀 `spec:`），与任何功能改动不混在同一分支/提交（R14）

---

## 0 · 立项信息（§12.2 步 1）

| 项 | 内容 |
|---|---|
| 编号 | **`J3.10`** —— §2.3 查重四条全空（**② 用新方法**：只匹配提交主题 ⇒ 0 条；旧方法会因 `J3.9` 提交正文的提及而误报） |
| **递增类型** | ★ **次版本位** `1.1.0 → 1.2.0` —— 三项都是**新增规则/澄清判定**，不改变任何红线 |
| 窗口 | `J3` 阶段已发布、`J4` 未开工 ⇒ 仍是 §12.5 第 4 条允许改规范的**唯一窗口**（与 `J3.8` 同一个窗口，`J4` 一开工即关闭） |

### 版本位判断（§12.3）

| 本次改动 | 性质 | 版本位 |
|---|---|---|
| §8.5 新增「变更类命令禁止管道截断」 | **新增禁忌** | 次版本位 |
| §2.3 查重 ② 改为只匹配主题 | **澄清判定**（使判定符合原意，非改变红线） | 次版本位 |
| §4⑥.1 新增「收尾结果回填」路径 | **新增流程** | 次版本位 |

**⇒ `1.2.0`**（无红线变动 ⇒ 不到主版本位）。

---

## 1 · 影响面清单（§12.2 步 2 ★ 必填）

### ① 受影响的章节

| 位置 | 改动 |
|---|---|
| 文件标题 + 元信息表 + §12.1 | `SPEC-1.1.0` → `SPEC-1.2.0`（三处版本号同步） |
| **§8.5（新增）** | 「★ 变更类命令**禁止**用管道截断输出」——现象 / 根因（`Select-Object -First N` 提前关闭管道终止上游）/ 正误写法对照 / **只读查询复核判据** / 判例 P7 实证 |
| **§2.3 查重 ②** | 命令由 `git log --oneline --grep="$NUM"` 改为 `git log --format="%s" \| grep -E "^(spec: )?${NUM//./\\.}[ ：:]"`（**只匹配提交主题**）；并补一段说明「提及 ≠ 占用」 |
| **§4⑥.1（新增）** | 「收尾实测结果的**回填**（轻量路径）」6 条规则：不另开编号 / 仍走独立分支 + `--no-ff` / **明确不违反 C2 与 C3** / 不打新 tag / `docs(<编号>):` 前缀 / 影响判据时须留痕 |
| 附录 B | 补入判例 **P7**（管道截断导致四条变更命令静默失效） |
| §12.4 | 追加 `1.2.0` 行（**只增不改**） |

### ② 会被重新解读的既有 tag / 分支 / 文档

| 对象 | 影响 | 结论 |
|---|---|---|
| 既有 `v*` tag（25 个） | **无** | 编号/序列规则未变（§4⑥.1 明确回填**不打新 tag**，正是为了不扰动 R8 的序号） |
| 既往任务的编号占用 | **无** | §2.3 ② 的新判定**只会减少误报**，不会让已占用的编号变成可用 |
| `docs/实施结果/J3.9-实施结果.md` | **本次新增的 §4⑥.1 正是为它的 §5 回填而设** | ✅ 之后按新路径执行（Branches `task/J3.9-tool-hardening-backfill`） |
| 判例 P1–P6 | **无删改** | 仅**追加** P7（§12.5 第 3 条禁止删改判例，追加不受限） |
| `scripts/**` | **无** | 零改动（纯文档） |

### ③ 与 `scripts/release.mjs` 的一致性影响（§12.2 步 3）

| 核对项 | 结果 |
|---|---|
| 子命令集合 | ✅ **零改动** |
| §8.5 的适用对象 | ✅ 描述的是 **PowerShell 管道行为**（与工具无关）⇒ 无同步问题 |
| §2.3 ② 的新命令 | ✅ 纯 git/grep，不经工具 ⇒ 无同步问题 |
| §4⑥.1 的 `-backfill` 分支 | ✅ 与 `cmdStart` 无关（回填不走 `task:start`，故不触发"分支已存在"检查）；`branch -d` 由人工在收尾时执行 |
| §11 命令表 | ✅ 无需改动 |

### ④ 与 `VERSIONING-QA.md` 的同步点

| 动作 | 说明 |
|---|---|
| **新增 Q20** | 「为什么'截断输出'会把 git 命令悄悄弄失效？」——含根因、只读 vs 变更的后果对照、三条规则、判例 P7 实证 |
| 顶部定位 | 已改为"版本与修订历史见 §12.4" ⇒ **本次无需改动** |

---

## 2 · 人类确认（§12.2 步 4 ★ 硬门槛）

| 项 | 内容 |
|---|---|
| 确认方式 | 用户在会话中明确指示：**「把 L12/L13/L14 补进规范」** |
| 确认前提 | 模型在上一轮完整列出三项遗留的性质、所属类别（均为 spec 修订）、以及"窗口即将因 `J4` 开工而关闭" |
| 授权范围 | 批准三项的**次版本位**修订（`1.1.0 → 1.2.0`） |
| 模型自行扩大范围？ | ❌ 无。未删改判例（P7 是追加）、未动 §1 红线、版本位判断从严 |

---

## 3 · DoD 核对

| # | 要求 | 落点 | 状态 |
|---|---|---|---|
| 1 | **L12** 管道截断坑写进规范 | §8.5（含正误对照 + 复核判据）+ 判例 P7 + Q20 | ✅ |
| 2 | **L13** 查重 ② 不再被"提及"误报 | §2.3 ② 改为只匹配主题 + 说明 | ✅ |
| 3 | **L14** 收尾结果回填有明确规定 | §4⑥.1（6 条规则，含 C2/C3 豁免声明） | ✅ |
| 4 | 三处版本号同步 | 标题 / 元信息表 / §12.1 | ✅ |
| 5 | 修订历史只增不改 | §12.4 追加 `1.2.0` | ✅ |
| 6 | Q&A 同步 | 新增 Q20 | ✅ |
| 7 | 走完 §12.2 七步 | 本文 + §12.4 | ✅ |

---

## 4 · 落点（文件清单）

| 文件 | 动作 |
|---|---|
| `docs/VERSIONING.md` | 修订：**新增 §8.5**、**新增 §4⑥.1**、§2.3 ② 及说明、附录 B 补 P7、§12.4；三处版本号 → `1.2.0` |
| `docs/VERSIONING-QA.md` | 修订：新增 Q20 |
| `docs/实施结果/J3.10-实施结果.md` | 新增（本文） |
| `package.json` | 修订：`version` → `0.3.0-dev.10` |

> **附带发布**：本任务交付后，可直接用新规定的 **§4⑥.1** 路径回填
> `docs/实施结果/J3.9-实施结果.md` §5（该表格因"收尾后才知道结果"而留空）
> —— 这将是 §4⑥.1 的**首次应用**，同时验证该路径可执行。

---

## 5 · 门禁项数（§6）

| # | 项 | 命令 | 结果 |
|---|---|---|---|
| 1 | 类型检查 | `node_modules/.bin/tsc --noEmit` | ✅ **通过**（3.4s） |
| 2 | 静态校验 | `node scripts/_verify.mjs` | ✅ **通过**（3.0s，7 脚本全过 / 0 跳过，含 `src/cabin/**` 逐字节摘要 → 源码零改动） |
| 3 | 构建 | `node_modules/.bin/astro build` | ✅ **通过**（4.3s） |

> 三项经**工具路径** `node scripts/release.mjs verify J3.10` 统一执行（含留痕）✓
| 4–6 | visual / smoke / perf | —— | ➖ **不适用**：改动**不含 `src/**` 任何字节**，由第 2 项的 `src/cabin/**` 逐字节摘要直接证明 |

---

## 6 · 遗留与交接

| # | 项 | 现状 |
|---|---|---|
| L12 / L13 / L14 | —— | ✅ **本次全部清掉** |
| L3 | `pre-commit` 受限沙箱崩溃 | 环境边界，无需执行（§11.1 已记录） |
| **L15**（新） | `docs/实施结果/J3.9-实施结果.md` §5 仍是 ⬜ | **按新增的 §4⑥.1 回填**（不另开编号、`-backfill` 后缀分支） |
| L8 | 待推送：`dev` 领先、`main` 领先 23 + 新 tag | 由 `ship` 生成、人工推送 |

---

## 7 · 流程自证（§12.2 七步）

| 步 | 实际执行 | 结果 |
|---|---|---|
| ① 立项 | §2.3 查重（**② 用新方法**）→ `start J3.10 spec-powershell-qa` | ✅ |
| ② 影响面清单 | 本文 §1（四项齐全） | ✅ |
| ③ 一致性核对 | 脚本零改动；三项修订都属"文档描述"层，无工具侧同步 | ✅ |
| ④ 人类确认 | 本文 §2 | ✅ |
| ⑤ 版本递增 | `SPEC-1.2.0`（次版本位） | ✅ |
| ⑥ 记录 | §12.4 追加一行（只增不改） | ✅ |
| ⑦ 独立提交 | 独立分支 + `--no-ff`，前缀 `spec:` | ✅ |
