# J4.32 · 任务 D 进度台账与交接 —— 实施结果

> 编号 **`J4.32`** ｜ 分支 `task/J4.32-task-d-progress-ledger` ｜ `--base=0.4.0`
>
> **本任务的产出是"进度台账"，不是代码 —— 代码零改动。**
> `J4.15-实施结果.md` 是「任务 D 的**清单与前置条件**」的权威文档；
> 本文是它的**进度更新**：`J4.18`–`J4.31` 共 **14 个任务**落地之后，
> 哪些组已关闭、哪些还开着、**每一组还缺哪一条前置**。

---

## 0. 一句话结论

任务 D 的 **15 组里已关闭 12 组**（10 组搬迁 + `decor-loop` 判定 + `magic-clock` 专项；
`J4.33`/`J4.34` 关掉 `teapot` 组、`J4.36` 关掉 `magic-book`）：

| 工作包 | 状态 |
|---|---|
| ① 光源前置（`lights(s)` 契约 + 槽位定序） | ✅ `J4.18`，并经 `J4.22` 证伪实验验证 |
| ② 批 1：九组无光源件 | 🟡 **8/9 组关闭** —— 只剩 `teapot` 组（前置已全就绪） |
| ③ 批 2：六件含光源件 | ✅ 一楼五件全搬（槽 0/1/2/3/4/7）；二楼两件（槽 5/6）待做且**必须同批** |
| ④ 专项：`magic-clock` 的 184 像素 | ✅ `J4.30` —— **零差异、专案关闭** |

> ★ **`J4.36` 更新（后出，优先）**：已关闭 **12 / 15**。**剩余 3 组** =
> **9** `astro`+`veil`+`star-particles`（须与 `candle` 同批）· **10** `board`（只剩编辑器归属裁决）·
> **15** `wand`（约 600 行，共享工具债已清）。
> 上表的「批 1 / 批 2」是按任务 D **原始计划口径**写的，**逐组实况一律以 §1 台账为准**
> （`teapot` 组在 `J4.34` 关闭 ⇒ ② 行"只剩 teapot 组"已过期，保留原文仅为留存计划轨迹）。

**当前 `dev` 状态**：`v0.4.0-dev.35`；工作区干净；`lights.js` 实测**剩 2 行**（都是二楼）。

---

## 1. 十五组状态台账（**本文的核心**）

| # | 组 | 状态 | 任务 / 备注 |
|---|---|---|---|
| 1 | `floor1/bookshelf` | ✅ 关闭 | `J4.19` |
| 2 | `floor1/cart-shelf` | ✅ 关闭 | `J4.20`（四段动画合并 + 新建判据 `test:cart`） |
| 3 | `floor1/crystal-ball` | ✅ 关闭 | `J4.25`（光源槽 4） |
| 4 | `floor1/hanging-lantern` | ✅ 关闭 | `J4.22`（光源槽 0；`J4.18` 契约的首个真实用户） |
| 5 | `floor1/kotatsu` | ✅ 关闭 | `J4.28`（光源槽 3；**最大单件**，229 行几何 + 三段 tick + 5 条交互） |
| 6 | `floor1/magic-circle` | ✅ 关闭 | `J4.24`（光源槽 2；96 次 rng 顺序保住） |
| 7 | `floor1/moon-plant` | ✅ 关闭 | `J4.23`（光源槽 7） |
| 8 | `floor1/teapot` 组 | ✅ 关闭 | `J4.33`（5 把椅子）+ `J4.34`（提梁茶壶）—— 曾判"互相咬合搬不动"的一组，**前置全部兑现** |
| 9 | **`floor2/astro`+`veil`+`star-particles`** + `candle`（槽 5） | 🟡 **3/4 已搬** | ✅ 决策已定（`J4.37`，**拆四件**）；`astro`（`J4.38`，**`magicP` owner**）· `candle`（`J4.39`，**槽 5 · 帧任务最分散**）· `veil`（`J4.40`，**槽 6 ⇒ `lights.js` 清空 · 批 2 收尾**）已就位 ⇒ 只剩 `star-particles` —— 见 §3.2 |
| 10 | **`floor2/board`** | 🟡 共享工具债已清 | 见 §3.3 |
| 11 | `floor2/desk-chair` | ✅ 关闭 | `J4.21` |
| 12 | `floor2/decor-loop` | ✅ 关闭（**判定**） | `J4.27` —— 它不是物件；四个成员已全部各归其位 |
| 13 | `floor2/magic-clock` | ✅ 关闭（**专项**） | `J4.30` —— 184 像素**零差异** |
| 14 | `floor2/magic-book` | ✅ 关闭 | `J4.36` —— **两处不相邻 tick 合并**的首个样板 |
| 15 | **`floor2/wand`** | 🟡 共享工具债已清 | 约 600 行（二楼最大）—— 见 §3.5 |

**范围外的光源件**：`floor2/candle`（槽 5）· `floor2/magic-veil`（槽 6）—— 见 §3.2。

---

## 2. 已落地的 19 个任务（`v0.4.0-dev.17` – `dev.35`）

| 任务 | 内容 | tag |
|---|---|---|
| `J4.18` | **光源前置**：`lights(s)` 契约真正接进 `lightField` + `slot` 槽位定序 | `dev.17` |
| `J4.19` | `floor1/bookshelf` | `dev.18` |
| `J4.20` | `floor1/cart-shelf`（+ 判据 `test:cart`） | `dev.19` |
| `J4.21` | `floor2/desk-chair` | `dev.20` |
| `J4.22` | `floor1/hanging-lantern`（槽 0） | `dev.21` |
| `J4.23` | `floor1/moon-plant`（槽 7） | `dev.22` |
| `J4.24` | `floor1/magic-circle`（槽 2） | `dev.23` |
| `J4.25` | `floor1/crystal-ball`（槽 4） | `dev.24` |
| `J4.26` | **共享件**：`props/cup.js`（茶杯工厂，解锁暖桌 + 茶壶） | `dev.25` |
| `J4.27` | `floor2/decor-loop` **单独判定**（关闭该组） | `dev.26` |
| `J4.28` | `floor1/kotatsu`（槽 3；**最大单件**） | `dev.27` |
| `J4.29` | `floor1/cauldron-fire`（槽 1）—— **一楼光源边界闭合** | `dev.28` |
| `J4.30` | **专项**：`floor2/magic-clock` —— **184 像素零差异** | `dev.29` |
| `J4.31` | **共享工具**：`smooth` / `hash01` / `jitterGeo` 提取到 `core/` | `dev.30` |
| `J4.32` | **进度台账与交接**（本文；代码零改动） | `dev.31` |
| `J4.33` | `floor1/dining-chairs`（5 把椅子）—— 段导出清单清零 | `dev.32` |
| `J4.34` | `floor1/teapot` —— **`teapot` 组关闭** | `dev.33` |
| `J4.35` | `floor2/magic-book` **搬迁施工图**（调研；代码零改动） | `dev.34` |
| `J4.36` | `floor2/magic-book` —— **两处不相邻 tick 的合并样板** | `dev.35` |
| `J4.37` | 组 9（`astro`+`veil`+`star-particles`+`candle`）**决策 + 施工图**（调研；代码零改动）| `dev.36` |
| `J4.38` | `floor2/astro` —— **组 9 开工，`magicP` owner 就位** | `dev.37` |
| `J4.39` | `floor2/candle` —— **帧任务最分散的一件（跨 35 个）· 槽 5 搬走 · 段导出清单归零** | `dev.38` |
| `J4.40` | `floor2/magic-veil` —— **槽 6 搬走 ⇒ `lights.js` 清空 · 批 2 收尾** | `dev.39` |

另有 **2 次回填**（`docs(J4.26):` / `docs(J4.28):`，按规范 §4⑥.1，不打新 tag）。

---

## 3. ★ 剩余 3 组：**每一组还缺哪一条前置**

> 这一节是本文最有交接价值的部分 —— 它把"能不能开搬"变成可核对的条件清单。
>
> **`J4.36` 更新**：本节原本覆盖 5 组。其中 **`floor1/teapot` 组**已由 `J4.33` + `J4.34` 关闭、
> **`floor2/magic-book`** 已由 `J4.36` 关闭 ⇒ §3.1 与 §3.4 两节**已过期**，
> 仅作计划轨迹留存（其前置分析对 `wand` / `board` 仍有参考价值）。
> **当前剩余 = §3.2（组 9）· §3.3（组 10）· §3.5（组 15）。**

### 3.1 ~~`floor1/teapot` 组~~ —— ✅ **已关闭（`J4.33` + `J4.34`）**

| 前置 | 状态 |
|---|---|
| `makeCup` 共享工厂（`cups[2]` 与暖桌共用同一集合） | ✅ `J4.26` |
| `_tv` 不再与暖桌抢（暖桌已自造私有临时量） | ✅ `J4.28` §1.3 |
| `chairs`（12.9f 的 5 把椅子）被 `movingPlatforms` 读 | ✅ 处置与 `stools` / `cartG` / `chairG` **完全同款**（改读 `parts`）—— 已有三次先例 |
| 长餐桌余段（`cups` 定义处） | ✅ 已由 `J4.26` 抽成工厂；`install.js` 仍持有 `cups` 实例 |

⚠️ 本组是**多件同段**（茶壶 + 3 只茶杯 + 5 把椅子），建议按 `floor1/doorHangBar` 的
"多段合成一件"先例处理，或按 `teapot` 与 `chairs` 拆两件。

### 3.2 `astro`+`veil`+`star-particles` **与** `candle`（槽 5）—— ✅ **决策已定（`J4.37`）**

**这是本阶段最需要提前知道的一条**（`J4.27` §6.2 查明、`J4.31` 复核）：

```js
// FrameBody 的每帧块是**交错**的：
frame/72  ctx.magicP += …            ← 18.5 星象仪的状态轴
frame/73  ctx.veil.material.opacity = ctx.magicP * 0.28;   ← 18.6 夜幕
frame/74  for (const cg of ctx.candleGlows) cg.m.material.opacity = … * ctx.candleP * boost * flick;
          //   ↑ boost = 0.30 + 0.50 * ctx.magicP  —— **蜡烛的辉光读了 veil 的状态**
frame/75+ spinG / innerG / glows / magicParts                ← 18.5 / 18.7
```

⇒ 把 `astro`+`veil`+`stars` 合并成一个 `update` 时，**必然把 `frame/74` 包进区间**，
而 `frame/74` 属于蜡烛 —— 反之单独搬蜡烛也会让 `magicP` 读到**上一帧的值**（画面必变）。

**前置状态**：✅ 光源槽 5 / 槽 6 的 `slot` 契约已由 `J4.18` 就绪；
✅ **决策已由 `J4.37` 做出 —— 拆四件**（既不是"合成一件"、也不是"拆两件"）。
原因：帧序等价性**不必靠"同批"来保证**，只需**登记顺序**正确
（`astro` 的 `update` 含 `frame/72`+`75`+`76`，排在 `candle` 的 `update` 之前
⇒ 所有 `magicP` 读者都拿到**本帧**值）。
⇒ ★ **"必须同批"的说法不成立：每件都能单独搬、单独过 `visual`。**
`magicP` 归 `astro`（它本来就是星象仪的状态轴），另两件经**装配选项**注入取用 ——
与 `J4.34` 的 `installProp(teapot, { ctx: { cupFactory } })` **同一手法**（已有先例）。

> ⚠️ `J4.37` 另查明一件**比上面更要紧**的事实：**`candle` 有 4 个帧任务**
> （`frame/35` / `70` / `71` / `74`），其中最远的 `frame/35`（L266）与次远的 `frame/70`（L528）
> **相隔 35 个帧任务** —— 这是任务 D 到目前为止**最分散**的一件（远超 `magic-book` 的"相隔 1 个"）。
> 其合并可行的依据是"`candleWavy` 的全部读者都在本件内部"（已 grep 全仓）。
>
> 完整帧序全表（9 条帧任务）、三条等价性判据、4 件的区间边界与符号清单、`rng` 清单
> （★ `star-particles` 单件消耗 **672 次** `floor2Rng`，全项目最多）、
> 以及**段导出/`ctx.*` 导出的读者排查表**，见 **`docs/实施结果/J4.37-实施结果.md`**。

### 3.3 `floor2/board` —— 共享工具债**已清**，只剩编辑器归属

| 前置 | 状态 |
|---|---|
| `smooth` 被 18.9 依赖（同一笔债） | ✅ **`J4.31` 已提取到 `core/math/easing.js`** |
| 便签编辑器（DOM）直接读计划板私有量 `notes` / `noteEditing` / `drawNote` | ⚠️ **待裁决**：应由 `features/**` 经 `mounts` 认领 `floor2/board` 的 `parts`（不变量 `N3`），而不是让 `install.js` 伸手进物件。`J4.27` §2.1 已确认编辑器代码**早已搬到** `systems/ui/editors/NoteEditor.js`，`install.js` 里只剩计划板自己的 `openNoteEditor(i)` |

### 3.4 ~~`floor2/magic-book`~~ —— ✅ **已关闭（`J4.36`）**

| 前置 | 状态 |
|---|---|
| `propCtx` 缺 `camera`（`updateGlyphs` 里 `g.m.quaternion.copy(camera.quaternion)`） | ✅ **`J4.8` 已补**（`propTool('camera', …)` / `propTool('renderer', …)`） |
| 259 行几何 + 两处**不相邻**的 tick（`updateBook(time, dt)` 与 `updateGlyphs(time, dt)`，中间夹 `updateCal(dt)`） | ⚠️ 一件物件只有一个 `update` ⇒ 需按 `J4.20`/`J4.28` 的"先论证两位置之间无读者/写者、再合并"模式处理 |
| `rng` | ⚠️ `spawnGlyphs` 每次 11 次 `runtimeRng()`、`updateGlyphs` 每帧可能 1 次 —— 次数与顺序必须不变 |

### 3.5 `floor2/wand` —— 共享工具债**已清**，仍需三件事

| 前置 | 状态 |
|---|---|
| `hash01` / `jitterGeo`（定义在 18.9 区间里、却是 `ctx` 共享工具） | ✅ **`J4.31` 已提取到 `core/geometry/jitter.js`** |
| `smooth`（由 18.8 提供、本件每帧用 3 处） | ✅ **`J4.31` 已提取** |
| `clock.now` → `s.now` | ⚠️ 照 `floor2/rubik.js` 的处置（二者恒等） |
| `runtimeRng` 的**次数与顺序** | ⚠️ `regMagic` 里 1 次、`buildCreation` 里数十次（**点击时**才调用 ⇒ 属运行期） |
| `scene.add` / `scene.remove` 的**动态增删顺序** | ⚠️ `updateWand2` 的 `clearCast()` 与施法段各有一处（渲染顺序敏感） |

---

## 4. 已建立的**可复用模式**（后续任务照抄即可）

### 4.1 光源物件（含槽位）的搬法 —— 已被 `J4.22`–`J4.29` 验证 6 次

1. 删 `world/lights.js` 里那一行；
2. 在物件里写 `lights(s, { L }) => [createPointLightSource({ id, **slot: N**, position, color, radius, strength, yMin, yMax })]`
   —— **位置/颜色/半径/yMin/yMax 逐字照搬**；
3. 强度改为读**物件自己的 `state`**（`() => s.pt`，或 `(time) => s.pt * (…)` **逐字照搬**原表达式）；
4. 把 `tickOnce()` 里那行 `ctx.ptXxx += …` 平滑**并进物件的 `update`**（见 §4.2）；
5. 删 `WeatherSystem.js` 里对应的 `ctx.ptXxx = …` 初值（**一楼已全部清零**）。

### 4.2 一份物件只有一个 `update`：两段帧任务怎么合并

**判据（每次都这么做，可 grep 复核）**：
① 两段在原 `FrameBody.js` 里**相邻**（中间无别的帧任务）⇒ 合并后每帧序列不变；
② 若把靠后那段**提前**：它是**一阶低通**、同帧同 `dt` 同输入 ⇒ 逐位相同，
且 `grep <状态量>` 的全部命中只有本件（外加 `frame/97` 才被调用的 `lights()` 闭包）
⇒ **两位置之间无读者/写者**；
③ 合并后**仍须早于消费者**（`lightField.update` 在 `frame/97`）。

> 反例记录：`candle` 的 `frame/74` **读 `ctx.magicP`** 而 `magicP` 由 `frame/72` 更新
> ⇒ 把蜡烛合并到它自己的几何分支位置（`frame/35`）会让 `magicP` 读到上一帧的值 ⇒ **画面必变**。
> **这就是 §3.2"必须同批"的由来。**

### 4.3 段导出清单（**连续五次拦下故障**）

`world/floorN/install.js` 顶部有一批**段导出**，形如
`ctx.xxx = xxx;` —— 它们靠**函数声明提升**引用本段的局部函数/变量。
**只要被引用的东西搬走/提取走，该分句就会在装配期抛 `ReferenceError`**（页面起不来）。

⇒ **动作**：搬任何东西之前，先 `grep "ctx\.<名字>"` 确认**有没有读者**；无读者就删该分句，
有读者就改指向新家。

已拦下 5 次：`J4.20` `startQuill` · `J4.26` `makeCup` · `J4.28` `makeCushion` ·
`J4.30` `clockHand`/`drawClock` · `J4.31` `hash01`/`jitterGeo`。

### 4.4 共享件的抽取形态（`props/README.md` 已记账）

- **纯造型** ⇒ 无状态工厂（返回新 `Group`）；
- **跨摆放者共享的行为** ⇒ `createXxxFactory(deps)`，**集合住在闭包**（不违反 `N7`），
  且**必须在文件头写明理由**（先例：`props/cup.js`）。
- **共享数学/几何工具** ⇒ 直接 `export`，由 `propCtx` 注入（先例：`core/math/easing.js`、
  `core/geometry/jitter.js`）。

---

## 5. 已知遗留与坑（**下一轮别再踩**）

| # | 项 | 说明 |
|---|---|---|
| 1 | **`verify-migration.mjs` 的 `stripComments` 不剥行注释** | 在**行注释**里写出那个「函数名 + 左括号」的字面量，会让"删掉 1 处 `regMagic(`"在判据上**被抵平**（`J4.19` 踩过、`J4.21` 连"解释这件事的注释"又踩一次；`J4.36` 是**第 7 次** —— 该件删掉 1 处真实 `regMagic(bookG, …)`，同时在新模块注释里写了同名文字量，故判据仍显示 `64 → 32 / -32`）。白名单现为 **`-32`**。**建议根治**（改它会牵动另外 7 个计数）| 
| 2 | **碰撞表的正确性没有自动判据** | `test:visual` 原理上无感、探针不看它。本阶段靠"数值逐字未变 + 用的是同一个实例"保证 |
| 3 | **动态行为多数无独立判据** | 只有 `cart-shelf` 补了 `test:cart`；其余靠探针（入口存在）+ 同构先例 |
| 4 | **PowerShell 的 `**` 通配不递归** | 引用检查**一律用 `grep` 工具**，不要用 `Select-String -Path src/cabin/**/*.js` |
| 5 | **`readyMs` 是本阶段最易噪声的指标** | 实测区间 −2.4% ~ +15.2%，紧跟长时冒烟之后可冲到 +42%（`J4.29` §4.1 有完整处置记录：**重跑 + 三条判据**） |
| 6 | **`decor-loop` 的两条驱动路径仍未合并** | `J4.27` §6.3；`J4.30` 已解除它与 `magic-clock` 的耦合 ⇒ 现在纯粹是调度问题 |
| 7 | ★ **`serve.mjs` 是 `dist` 模式 ⇒ `build` 必须排在所有"需要真实页面"的门禁之前** | 5175 上服务的是 `astro build` 的**产物**（`serve.mjs` L12 / L40），不是源码实时转换。若先跑 `visual` 再 `build`，`visual` 验证的是**上一个任务**的产物 —— **假通过**（`J4.36` 踩过：`J4.38` 任务中查明。暴露方式是 `build` 之后 `diag` 立刻报 `ReferenceError`、`props` 数量骤降、`ready` 超时）。**正确顺序：`tsc → verify → ★build → visual → smoke → perf → diag → probe`**。若 `visual` "通过得太顺"，先确认 `dist/` 是刚构建的。`J4.36` 已在 §8 补做正确顺序的复核（3/3 逐字节相同），结论不受影响 |
| 8 | ★ **用 `pwsh` 改文件后，紧接着的 `edit` 会全部失败（"file changed since it was read"）** | 重做时**不要凭记忆补**：把每一条失败的 `edit` 当成待办**逐条重做**。`J4.38` 因此漏掉一条（从段导出清单删 `ctx.glowBall`）⇒ 留下指向已删除函数的**悬空导出**，页面启动即 `ReferenceError`，而 **`tsc` 与 `build` 都没拦住**（函数提升让未定义的名字逃过静态检查）。**更稳的做法**：改文件要么全用 `edit`、要么全用 `pwsh`；**合并/删除整整一段之后，必须再 `grep` 一次被删段的全部符号名** |

---

## 6. 下一步建议（按"前置完备度"排序）

1. **`teapot` 组** —— 前置**全部就绪**（§3.1），一次可推进任务 D 的第 8 组；
2. **`magic-book`** —— 前置已就绪（§3.4），只需处理"两处不相邻 tick 的合并论证"；
3. **`astro`+`veil`+`stars` + `candle` 四件** —— ✅ **决策已由 `J4.37` 做出（拆四件）**，
   施工图（帧序全表 + 三条判据 + 符号清单 + rng 清单）已就绪 ⇒ **可直接开工**，
   建议先做 `astro`（它是 `magicP` 的 owner，另两件依赖它），且**每件可单独过 `visual`**；
4. **`board`** —— 需先裁决便签编辑器的归属（§3.3）；
5. **`wand`** —— 规模最大（600 行）+ 三项运行期约束（§3.5），建议放最后。

---

## 7. DoD 核对与落点

| 本任务自定 DoD | 结果 |
|---|---|
| 给出 15 组的**当前状态台账** | ✅ §1 |
| 给出**每一剩余组还缺哪一条前置** | ✅ §3（本文的核心） |
| 固化**可复用的搬法模式** | ✅ §4（光源物件 / 两段合并判据 / 段导出清单 / 共享件形态） |
| 记下**已知遗留与坑** | ✅ §5 |
| 给出**下一步建议**（按前置完备度排序） | ✅ §6 |
| 代码零改动、画面逐字节不变 | ✅ §8 门禁 |

### 7.1 落点

| 文件 | 动作 |
|---|---|
| `docs/实施结果/J4.32-实施结果.md` | **新增**：本文（本任务唯一产出 —— 代码零改动） |
| `package.json` | `version` → `0.4.0-dev.31`（R9） |

> **为什么可以"只交文档"**：与 `J4.15` 同一处置 —— 它把「35 件的真实边界与前置条件」
> 固化成可交接的证据；本文把「14 个任务之后的进度与**剩余每一组还缺什么**」固化下来。
> 两者合起来，任何新会话都能在**不重读 15 份 `SKIP.md`** 的前提下接着做。

---

## 8. 门禁（六项）

| # | 命令 | 结果 |
|---|---|---|
| 1 | `tsc --noEmit` | ✅ 0 错误 |
| 2 | `node scripts/_verify.mjs` | ✅ 58 项通过 |
| 3 | `astro build` | ✅ 8 page(s) built |
| 4 | **画面回归** | ✅ **3/3 机位 sha256 逐字节相同**（代码零改动 ⇒ 必然，仍实测留痕） |
| 5 | 冒烟 | ✅ 见 §8.1 |
| 6 | 性能 | ✅ 见 §8.1 |

### 8.1 冒烟 / 性能

| 项 | 结果 |
|---|---|
| 冒烟 | ✅ **32 通过 / 0 失败**（250.7s，无未捕获页面异常） |
| 性能 | ✅ 全部指标在阈值内；`readyMs` +14.8%、`fps` −10.4%（均为参考项），五项场景统计**全部 +0.0%** |
| `verify` 的特别之处 | ✅ **58 项零失败**（代码零改动 ⇒ `cabin-digest` 未触发，本阶段唯一一次不带预期红项的收尾） |
