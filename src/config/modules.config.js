/**
 * ★ 模块总开关表（决策 5 的核心）
 *
 * 对齐 Firefly 的 `siteConfig.pages.*`：**一行关掉一个功能模块**。
 * 键 = docs/BuildPlaning/mapping.yaml 的模块 id（那个文件是映射真源，本文件只管开关）。
 *
 * 置 `false` 后发生三件事（验收 BB3 / CF1）：
 *   ① 场景侧：它的 interactable 不注册，物品恢复纯装饰，点击给出「这件东西还没启用」浮签
 *   ② 站点侧：它的页面 / 面板不生成（对齐 Firefly 的"返回 404 并隐藏导航项"）
 *   ③ 加载侧：它的 src/features/<id>/** 不被 import、不被下载
 *
 * 环境变量覆盖（部署时无需改文件）：
 *   PUBLIC_MODULES=M01,M02,M03a,M04      ← **白名单语义**：列出的启用，其余一律关闭
 *   未设置时使用下面的默认值，一个都不改。
 */
import { resolveModules } from './resolve.js'

/** @type {import('./types.js').ModulesConfig} */
export const modulesConfig = resolveModules({
  // ── P1 内容骨架（由 J6 落地）────────────────────────────────
  M01: true, // 内容存档 → 一楼左墙三层书架 + 左窗下书堆（★ 核心，不可关）

  // ── P2 内容发现 ────────────────────────────────────────────
  M02: true, // 时间归档 → 毛茸茸大地毯（年历）+ 二楼台历
  M03a: true, // 搜索     → 一楼水晶球占卜台
  M03b: true, // 随机一篇 → 二楼扑克牌堆
  M04: true, // 主题聚类 → 左墙试剂药水架（7 瓶）

  // ── P3 社交面 ──────────────────────────────────────────────
  M07: true, // 个人名片与导航 → 二楼魔女帽 + 一楼月光魔法盆栽
  M08: true, // 评论互动       → 抽纸盒（写）+ 计划板便签（看）
  M10: true, // 照片相册       → 前墙相框组 + 玻璃雪景球（彩蛋）

  // ── P4 氛围与个性化 ────────────────────────────────────────
  M11: true, // 音频播放 → 一楼暖桌上的收音机
  M13: true, // 站点统计 → 二楼烟囱墙魔法时钟
  M16: true, // 字体设定 → 滑轮置物台上层的墨水瓶 + 羽毛笔
  M17: true, // 显示设定 → 二楼床头柜抽屉（★ 设置面板的落点）

  // ── P5 外部世界（依赖外部服务，默认关）─────────────────────
  M12: false, // 影音收藏 → 星象仪（4 个平台在源站里默认全关，Q10）
  M14: false, // 热点榜单 → 旋转星铃（依赖内网 HotSpot 服务，Q17）
  M15: true, // 收藏导航 → 屋外路牌

  // ── P6 长尾收尾 ────────────────────────────────────────────
  M05: false, // 创作管线 → 滑轮置物台 + 抽纸盒 + 垃圾桶（3D 里写作意义有限，Q14）
  M06: true, // 关于本站 → 二楼书桌上的魔法书本
  M18: false, // 背单词   → 大魔女坩埚（通道 A 的首个真实用例，最后做）

  // ── 不映射到场景 ───────────────────────────────────────────
  M09: false, // 打赏：只在文章页底部保留按钮，场景中不出现（Q15）
})

// ⚠️ 门禁（J2.5 实现，验收 CF3 / 风险 R30）：
//   validateModuleToggles(modulesConfig, mappingIds) —— 与 mapping.yaml 的 id 集合不一致即构建失败。
//   validateConfigFiles(modulesConfig, 'src/config') —— 每个已上线模块必须有对应的配置文件，
//   缺一个即构建失败（防止"删了配置还以为是默认值"）。
